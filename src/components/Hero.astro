<section id="home" class="min-h-screen flex items-center justify-center relative overflow-hidden">
  <!-- Animated Background with CSS-only 3D sphere effect -->
  <div class="absolute inset-0 z-0">
    <div class="hero-3d-background">
      <div class="animated-sphere"></div>
      <div class="sphere-glow"></div>
    </div>
  </div>

  <!-- Animated Background Particles -->
  <div class="absolute inset-0 z-5">
    <div class="floating-particles">
      <!-- Particles will be generated by JavaScript -->
    </div>
  </div>

  <!-- Content -->
  <div class="relative z-10 text-center px-4 max-w-4xl mx-auto">
    <div class="scroll-animation mb-6" data-animation="fade-up">
      <div class="inline-block p-1 bg-gradient-to-r from-red-400 to-pink-400 rounded-full mb-4">
        <div class="bg-slate-900 rounded-full px-6 py-2">
          <span class="text-red-400 font-semibold">👋 Hello, I'm</span>
        </div>
      </div>
      <h1 class="text-5xl md:text-7xl font-bold mb-4 hero-text-glow">
        <span class="bg-gradient-to-r from-red-400 via-pink-400 to-purple-400 bg-clip-text text-transparent animate-gradient">
          Kevin Arauz
        </span>
      </h1>
      <h2 class="text-3xl md:text-4xl font-semibold text-white mb-2">Full Stack Developer & Software Engineer</h2>
    </div>

    <div class="scroll-animation" data-animation="fade-up" data-delay="200">
      <p class="text-xl md:text-2xl text-gray-300 mb-8 leading-relaxed">
        Passionate programmer and systems engineering student with a strong commitment to turn
        <br />
        technology into practical and creative solutions
      </p>
    </div>

    <!-- Social Links -->
    <div class="scroll-animation" data-animation="fade-up" data-delay="300">
      <div class="flex justify-center gap-4 mb-8 flex-wrap">
        <a href="https://github.com/kjarj54" target="_blank" rel="noopener noreferrer" class="social-button" aria-label="GitHub Profile">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="currentColor">
            <path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/>
          </svg>
          <span class="social-tooltip">GitHub</span>
        </a>
        <a href="https://linkedin.com/in/arauzkj" target="_blank" rel="noopener noreferrer" class="social-button" aria-label="LinkedIn Profile">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="currentColor">
            <path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433c-1.144 0-2.063-.926-2.063-2.065 0-1.138.92-2.063 2.063-2.063 1.14 0 2.064.925 2.064 2.063 0 1.139-.925 2.065-2.064 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/>
          </svg>
          <span class="social-tooltip">LinkedIn</span>
        </a>
        <a href="mailto:kjarj14@gmail.com" class="social-button" aria-label="Send Email">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="m4 4 7.07 7.07a2 2 0 0 0 2.86 0L21 4"/>
            <path d="M21 4v13a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V4"/>
          </svg>
          <span class="social-tooltip">Email</span>
        </a>
        <a href="https://drive.google.com/file/d/1DFqJojxnrsncCsbQzq4sEhvjJlFS6m7C/view?usp=sharing" target="_blank" download class="social-button" aria-label="Download CV">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/>
            <polyline points="7,10 12,15 17,10"/>
            <line x1="12" y1="15" x2="12" y2="3"/>
          </svg>
          <span class="social-tooltip">Download CV</span>
        </a>
      </div>
    </div>

    <div class="scroll-animation" data-animation="fade-up" data-delay="400">
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <button
          id="viewWorkBtn"
          class="cta-button primary"
        >
          View My Work
        </button>
        <button
          id="contactBtn"
          class="cta-button secondary"
        >
          Get In Touch
        </button>
      </div>
    </div>
  </div>

  <!-- Scroll Indicator -->
  <div class="absolute bottom-8 left-1/2 transform -translate-x-1/2 animate-bounce">
    <div class="scroll-indicator">
      <div class="scroll-dot"></div>
    </div>
  </div>
</section>

<script>
  import '../scripts/scroll-animation.ts';
  import { smoothScrollTo } from '../scripts/scroll-animation.ts';

  // Generate floating particles
  function createFloatingParticles() {
    const particlesContainer = document.querySelector('.floating-particles');
    if (!particlesContainer) return;

    // Clear existing particles
    particlesContainer.innerHTML = '';

    // Create 20 particles
    for (let i = 0; i < 20; i++) {
      const particle = document.createElement('div');
      particle.className = 'particle';
      particle.style.left = `${Math.random() * 100}%`;
      particle.style.animationDelay = `${Math.random() * 20}s`;
      particle.style.animationDuration = `${15 + Math.random() * 10}s`;
      particlesContainer.appendChild(particle);
    }
  }

  // Handle CTA button clicks
  function setupCTAButtons() {
    const viewWorkBtn = document.getElementById('viewWorkBtn');
    const contactBtn = document.getElementById('contactBtn');

    viewWorkBtn?.addEventListener('click', () => {
      smoothScrollTo('#projects');
    });

    contactBtn?.addEventListener('click', () => {
      smoothScrollTo('#contact');
    });
  }

  // Initialize everything
  function init() {
    createFloatingParticles();
    setupCTAButtons();
  }

  // Run on both initial load and client-side navigation
  document.addEventListener('DOMContentLoaded', init);
  document.addEventListener('astro:page-load', init);
</script>

<style>
  /* 3D Background Sphere Effect */
  .hero-3d-background {
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
  }

  .animated-sphere {
    width: 24rem;
    height: 24rem;
    border-radius: 9999px;
    position: relative;
    background: conic-gradient(from 0deg at 50% 50%, #800020, #ff6b9d, #800020);
    animation: rotateGradient 10s linear infinite, spherePulse 4s ease-in-out infinite;
    filter: blur(2px);
  }

  .sphere-glow {
    position: absolute;
    inset: 0;
    width: 24rem;
    height: 24rem;
    border-radius: 9999px;
    opacity: 0.3;
    background: radial-gradient(circle, #800020 0%, transparent 70%);
    animation: glowPulse 3s ease-in-out infinite alternate;
  }

  @keyframes rotateGradient {
    0% {
      transform: rotate(0deg) scale(1);
    }
    50% {
      transform: rotate(180deg) scale(1.1);
    }
    100% {
      transform: rotate(360deg) scale(1);
    }
  }

  @keyframes spherePulse {
    0%, 100% {
      transform: scale(1);
      opacity: 0.8;
    }
    50% {
      transform: scale(1.05);
      opacity: 1;
    }
  }

  @keyframes glowPulse {
    0% {
      transform: scale(0.8);
      opacity: 0.3;
    }
    100% {
      transform: scale(1.2);
      opacity: 0.1;
    }
  }

  /* Enhanced hero text glow */
  .hero-text-glow {
    text-shadow: 
      0 0 20px rgba(239, 68, 68, 0.5),
      0 0 40px rgba(239, 68, 68, 0.3),
      0 0 60px rgba(239, 68, 68, 0.2);
  }

  /* Responsive adjustments */
  @media (max-width: 768px) {
    .animated-sphere,
    .sphere-glow {
      width: 16rem;
      height: 16rem;
    }

    .social-button {
      padding: 0.5rem;
    }

    .hero-text-glow {
      text-shadow: 0 0 10px rgba(239, 68, 68, 0.5);
    }
  }

  @media (max-width: 640px) {
    .animated-sphere,
    .sphere-glow {
      width: 12rem;
      height: 12rem;
    }
  }

  /* Performance optimizations */
  .animated-sphere,
  .sphere-glow {
    will-change: transform;
  }

  /* Accessibility: Respect reduced motion preferences */
  @media (prefers-reduced-motion: reduce) {
    .animated-sphere,
    .sphere-glow {
      animation: none !important;
    }
    
    .animate-bounce {
      animation: none !important;
    }
    
    .animate-gradient {
      animation: none !important;
    }
  }
</style>
